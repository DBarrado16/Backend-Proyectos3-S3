PORT=3000

# CORS — orígenes permitidos (separados por coma). Vacío = permite todo
CORS_ORIGINS=https://tu-front.vercel.app,http://localhost:5173

# JWT — autenticación
JWT_SECRET=cambia-esto-por-un-secreto-seguro-en-produccion
ADMIN_USERNAME=admin
ADMIN_PASSWORD=cambia-esto-en-produccion

# OpenRouter (LangChain)
OPENROUTER_API_KEY=tu_api_key_aqui
OPENROUTER_MODEL=google/gemma-4-26b-a4b-it:free

# Telegram
TELEGRAM_BOT_TOKEN=

# Email (SMTP / SendGrid)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=
EMAIL_FROM=notificaciones@app.com
